mutation AdminLogin($phoneNumberOrEmail: String!, $password: String!) {
  adminLogin(input: { phoneNumberOrEmail: $phoneNumberOrEmail , password: $password }) {
    id
    email
    username
    token
    role
  }
}